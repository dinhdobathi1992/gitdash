services:
  gitdash:
    build:
      context: .
      dockerfile: Dockerfile
    image: gitdash:latest
    container_name: gitdash
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
